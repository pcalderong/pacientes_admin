<%= form_for(@historial_clinico) do |f| %>
  <% if @historial_clinico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@historial_clinico.errors.count, "error") %> prohibited this historial_clinico from being saved:</h2>

      <ul>
      <% @historial_clinico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :fk_id_persona %><br>
    <%= f.number_field :fk_id_persona %>
  </div>
  <div class="field">
    <%= f.label :fecha %><br>
    <%= f.datetime_select :fecha %>
  </div>
  <div class="field">
    <%= f.label :fk_id_lookup %><br>
    <%= f.number_field :fk_id_lookup %>
  </div>
  <div class="field">
    <%= f.label :orden %><br>
    <%= f.number_field :orden %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
